services:
  - type: web
    name: sistema-eleitoral-rondonia
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
